package com.noura.promotion.domain.enums;

/**
 * Entity scopes that can constrain promotion eligibility.
 */
public enum PromotionApplicableEntityType {
    CATEGORY,
    PRODUCT,
    VARIANT,
    COLLECTION
}
