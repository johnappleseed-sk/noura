package com.noura.shipping.domain.enums;

/**
 * Supported merchant lifecycle states for admin compatibility flows.
 */
public enum MerchantStatus {
    DRAFT,
    ACTIVE,
    SUSPENDED,
    INACTIVE
}
